# Slice 5A - Orchestrator (Alice + Bob + demo glue).
#
# Inherits everything from requirements-memory.txt + requirements-darkpool.txt;
# the only NEW dep is sentence-transformers for real text embeddings of Alice's
# seeded corpus + Bob's market_state queries.

-r requirements-memory.txt
-r requirements-darkpool.txt

sentence-transformers>=3.0,<6.0

# Real LLM duelists (The Colosseum). The duel runner makes live model calls;
# the key is read from $ANTHROPIC_API_KEY (never argv). Stubbed in unit tests.
anthropic>=0.40,<1.0
