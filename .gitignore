node_modules/
.env
.env.local
.env.*
!.env.example
dist/
build/
.DS_Store

# Secrets / keys (defense-in-depth — never commit these)
*.keystore
keystore/
*.mem
*.key
*.secret

/docs
/bench

# Claude Code dev worktrees (local-only, can be large)
.claude/worktrees/

# Python bytecode and caches
**/__pycache__/
*.pyc
.pytest_cache/

# Playwright MCP debug artifacts
.playwright-mcp/

# Local-only backups and screenshots
*.localfork.bak
Screenshot*.png
image.png
